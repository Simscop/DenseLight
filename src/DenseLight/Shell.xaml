<Window
    x:Class="DenseLight.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:liftui="https://github.com/pchuan98/Lift.UI"
    xmlns:local="clr-namespace:DenseLight.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:DenseLight.ViewModels"
    Title="Shell"
    Width="1200"
    Height="800"
    d:DataContext="{d:DesignInstance Type=viewModel:ShellViewModel}"
    mc:Ignorable="d">

    <Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <Grid>
                <liftui:ImageViewer 
                    Margin="5"
                    BorderBrush="SeaGreen"
                    ImageSource="{Binding }"
                    BorderThickness="1" />
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid>
                    <StackPanel Orientation="Vertical">
                        <TextBlock
                            Margin="5"
                            HorizontalAlignment="Center"
                            FontSize="12"
                            FontWeight="Bold"
                            Text="AutoFocus Measurement Demo" />

                        <local:Steer x:Name="SteerControl" />

                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1">
                    <local:HikCamera x:Name="HikCameraControl" />
                </Grid>
            </Grid>
        </Grid>
    </Border>

</Window>
